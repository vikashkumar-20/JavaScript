// Window is a global object and any operations related to entire browser window can be part of window object
// All the members like methods, object or properties. It's a part of window object.
//windows has methods and properties like setTimeOut(), setInterval().\

// BOM and DOM are the part of window object. 

//BOM: Browser object model: In which we deal with navigator, url's, ssl, screen, frames, history etc

//DOM: Document object model: If i want to change in document such as text, attribute, elements. 
//or we can say in html, then we can change with the help of DOM. (Content of a web page)

/* 
                        DOM Properties

                        document

                        getElementById(id)

                        getElementsByClassName(className)

                        getElementsByTagName(tagName)

                        querySelector(selector)

                        querySelectorAll(selector)

                        innerHTML

                        textContent

                        style
*/


/*
                DOM Methods

                createElement(tagName)

                appendChild(node)

                removeChild(node)

                addEventListener(event, function)

                removeEventListener(event, function)

                setAttribute(name, value)

                getAttribute(name)

                parentNode / parentElement

                childNodes / children

                firstChild / firstElementChi

                lastChild / lastElementCh

                nextSibling / nextElemen

                previousSibling / previo

                closest(selector)

                forEach (Array.from)
*/



